
repositories {
    mavenCentral()
}

group = 'com.jasur'
version = '1.0'

dependencies {
    implementation project(':utils')
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')